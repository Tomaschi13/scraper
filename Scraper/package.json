{
  "name": "scraper-ide",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension IDE for a portal-backed scraper workflow.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "jsdom": "^22.1.0"
  }
}
